#!/bin/bash
# chenwu@espressif.com
set -e

REPO_PATH="$(dirname "$(dirname "$(realpath "${BASH_SOURCE[0]}")")")"

for sh in $REPO_PATH/common/*
do {
    source $sh
}
done
wait

for component in $REPO_PATH/components/*
do {
    export PATH=$component:$PATH
}
done
wait
